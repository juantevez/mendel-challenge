server:
  port: 0  # Random port

spring:
  application:
    name: mendel-challenge-test

  data:
    redis:
      timeout: 2000ms

redis:
  enabled: true

logging:
  level:
    com.mendel: DEBUG
    org.springframework.data.redis: DEBUG
    org.testcontainers: INFO

